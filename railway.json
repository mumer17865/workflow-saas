{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workflow/api exec prisma generate && pnpm --filter @workflow/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @workflow/api exec prisma migrate deploy && node apps/api/dist/main.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
