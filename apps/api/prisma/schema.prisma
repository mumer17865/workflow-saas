// WorkFlow — Prisma schema
// Phase 1: datasource + generator only. Domain models arrive in Phase 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
