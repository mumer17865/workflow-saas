# Normalize line endings: store as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# Generated / lockfiles — keep out of language stats
pnpm-lock.yaml linguist-generated=true
